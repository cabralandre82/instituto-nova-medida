# PRs Pendentes — Inputs Aguardando do Operador

Lista consolidada de PRs identificados na auditoria (`docs/AUDIT-FINDINGS.md`) que **não podem ser abertos só pelo engenheiro**: dependem de dados reais, decisões do operador ou acesso externo.

Atualizado em: **2026-04-20** (pós-PR-041-B / D-085 — migração Next 14.2.35 → 15.5.15 + React 18 → 19, fechando os 4 advisories DoS da linha Next 14 listados como follow-up do finding [11.1] [Image Optimizer SSRF, RSC deserialization, rewrite header smuggling, next/image cache poisoning]; codemod `next-async-request-api` + refactor manual de `supabase-server.ts` pra `async` factories sem `UnsafeUnwrappedCookies`; `<a href>` que virou `<Link>` em `NewDoctorForm` pra fechar regra ESLint 15 estrita. Build produção OK, 1440 testes verdes, 0 TSC errors, 0 vulnerabilities. Revisão anterior: PR-070-B / D-084 — UI `/admin/magic-links`).

---

## PR-023 · Preencher CNPJ + Responsável Técnico Médico no footer

**Status:** ⏸️ **BLOQUEADO — aguardando dados do operador**
**Severidade do finding:** 🔴 CRÍTICO (audit [7.1])
**Arquivo afetado:** `src/components/Footer.tsx`

### Por que é CRÍTICO

O rodapé público hoje contém literalmente `CNPJ [a preencher]` e `Responsável Técnico Médico: Dra. [Nome], CRM/[UF] [número]`.

- **CFM 2.314/2022** exige identificação clara do responsável técnico em qualquer site de telemedicina.
- Sem CNPJ real: auditor fiscal externo ou Procon podem autuar.
- Com tráfego pago ativo + placeholder visível = infração direta CFM + LGPD (Art. 9º) + CDC Art. 31.

Enquanto o placeholder estiver lá, **não é recomendado publicidade paga nem veiculação em redes**.

### O que preciso do operador

Copie-cole na próxima mensagem:

```text
CNPJ da pessoa jurídica (clínica): ________________
Razão social completa: ________________
Responsável Técnico Médico — nome completo: ________________
CRM (somente número): ________________
CRM UF (estado da inscrição, ex: "SP"): ________________
E-mail institucional do DPO (privacidade@...): ________________
Número de WhatsApp comercial (apenas dígitos, com DDI 55): ________________
Endereço físico da sede (rua, número, bairro, cidade, UF, CEP): ________________
```

### O que será feito quando chegar

1. Substituir placeholders em `src/components/Footer.tsx`.
2. Mover os valores fixos para `src/config/legal.ts` (centralizado, tipado, com JSDoc citando CFM 2.314/2022).
3. Acrescentar smoke test `src/components/Footer.test.tsx` que **falha o build** se qualquer placeholder (`[a preencher]`, `[Nome]`, `[número]`, `[UF]`) for re-introduzido.
4. Registrar ADR no `docs/DECISIONS.md` (D-048).
5. Validar via `rg "\\[a preencher\\]|\\[Nome\\]|\\[n[úu]mero\\]|\\[UF\\]" src/` devolver zero resultados.

---

## Outros PRs com dependência de decisão do operador (não CRÍTICOS)

### PR-022 (CANCELADO) · Crons UTC→BRT

- **Status original:** 🔴 CRÍTICO proposto na auditoria.
- **Status atual:** ♻️ **Cancelado em 2026-04-20.**
- **Motivo:** Verificação pós-auditoria confirmou que os schedules `vercel.json` **já estão corretos em UTC** e cada `route.ts` documenta a conversão UTC→BRT. Não era bug, é preferência operacional.
- **Follow-up opcional:** se o operador quiser ajustar horários (ex.: mover `admin-digest` de 08:30 BRT para 09:00 BRT), abrir PR isolado trivial no `vercel.json`.

### PR-033 (parte B) · DPA com farmácia parceira

- **Status:** ⏸️ Aguardando operador (audit [6.6]).
- **O que preciso:**
  - Qual farmácia de manipulação é a parceira oficial?
  - Existe Data Processing Agreement (DPA) ou Termo de Operador LGPD assinado?
  - Se não, precisa elaborar. Recomendo consultar advogado + template da ANPD.
- **Bloqueante para:** ativação completa da rotulagem "parceiro LGPD".

### PR-038 · 2FA obrigatório para admin

- **Status:** ⏸️ Aguardando decisão do operador (audit [3.X PARTE 1]).
- **O que preciso:**
  - Operador prefere TOTP (Google Authenticator / Authy) ou Passkey (WebAuthn)?
  - Telefone secundário para recovery (caso perca device)?
- Supabase já suporta TOTP nativo (já está em `config.toml` com `enroll_enabled=true`), falta só forçar o fluxo obrigatório para role=admin.

### PR-046 · Multi-médica (quando chegar a 2ª)

- **Status:** ⏸️ Fora de escopo até existir 2ª médica contratada (audit [12.1]).
- **O que será necessário:**
  - Schema de `doctor_specialties` + routing por prioridade.
  - UI admin de triagem (atribuir paciente a médica).
  - Split financeiro por médica atendente vs RT.

### PR-047 · Break-glass account

- **Status:** ⏸️ Aguardando operador (audit [3.X PARTE 1]).
- **O que preciso:**
  - Qual e-mail secundário (fora de `cabralandre@yahoo.com.br`) para conta de emergência?
  - Cofre físico / 1Password / Bitwarden onde a credencial fica lacrada?
- Sem isso, se o Yahoo Mail cair ou a conta for comprometida, o operador perde acesso irrecuperável à plataforma.

---

## Cadência recomendada

Enquanto o operador colhe os dados acima, o engenheiro segue com os PRs que não dependem de inputs externos.

**Status pós-PR-074 (2026-04-20):**

- ✅ Concluídos:
  - PR-041-B (D-085): **migração Next.js 14.2.35 → 15.5.15 + React 18 → 19** — fecha os **4 advisories DoS da linha Next 14** que estavam listados como "Follow-up detectado (novo) — PR-041-B" dentro do finding [11.1] em `AUDIT-FINDINGS.md` linha 1600 (Image Optimizer SSRF, RSC deserialization DoS, rewrite header smuggling, next/image cache poisoning via filename collision). O finding [11.1] propriamente já estava ✅ RESOLVED em PR-041/D-060. Estratégia em 3 fases: **(1)** codemod `next-async-request-api` analisou 340 arquivos, modificou só 4 (`crons/page.tsx`, `errors/page.tsx`, `health/page.tsx` + `supabase-server.ts`) — os demais 336 já estavam migrados incrementalmente ao longo de Sprint 2 (PRs 053/056/061/067/070/072/073/074/070-B/073-B/C escritos em Next-15-style por disciplina). **(2)** Refactor manual de `src/lib/supabase-server.ts`: codemod tinha substituído `cookies()` por `UnsafeUnwrappedCookies` (escape hatch deprecated em Next 16); rejeitado e reescrito como `export async function getSupabaseServer()` / `getSupabaseRouteHandler()` retornando `Promise<SupabaseClient>` com `await cookies()` dentro. Propaguei `await` pros 4 call-sites (`auth.ts::getSessionUser`, `auth.ts::requireDoctor`, `api/auth/callback/route.ts`, `api/auth/signout/route.ts`). Zero `UnsafeUnwrappedCookies` no tree. **(3)** Bump `next@15.5.15 eslint-config-next@15.5.15 react@19.2.5 react-dom@19.2.5 @types/react@19 @types/react-dom@19` + 4 gates de validação: TSC 0 erros, ESLint 1 erro trivial corrigido (`<a href="/admin/doctors">` em `NewDoctorForm.tsx` virou `<Link>` — regra `@next/next/no-html-link-for-pages` mais estrita em ESLint 15), Vitest 1440/1440 verdes, `next build` produção OK. Caching defaults do Next 15 (GET Route Handlers + Client Router Cache não-cacheados por default) irrelevantes: 95 call-sites já declaravam `export const dynamic = "force-dynamic"`. React 19 backward-compatible; framer-motion 11.18.2 já tem peer range `^18 || ^19`. Escolha 15.5.15 (não 15.6 canary nem 16.2.4) pela maturidade operacional — 15.5 tem 3 meses de prod em ecossistemas maiores, 16 vira PR-041-C futuro se houver advisory específico. Warning cosmético de `outputFileTracingRoot` (multiple lockfiles em pais) deixado pra depois — build funciona, correção arrisca cascata em CI. **Risco residual baixo-médio**: concentrado no caminho de auth (mudança de assinatura de `getSupabaseServer`), mitigado por build + vitest + TSC verdes. Smoke manual pós-merge em 4 rotas (admin login, patient login, checkout webhook, fulfillments) cobrido pelo RUNBOOK-PRODUCTION-CHECKLIST §10. Artefatos em D-085.
  - PR-070-B (D-084): **UI admin de trilha forense de magic-link em `/admin/magic-links`** — fecha follow-up do PR-070/D-078 que vinha sendo triado via SQL inline no Studio. Página server-only em `src/app/admin/(shell)/magic-links/page.tsx` consumindo `magic_link_issued_log` read-only com filtros canônicos via `admin-list-filters.ts`: busca por **email** (plaintext no input, servidor hasheia via `hashEmail()` antes do `WHERE email_hash = ?` — email plaintext **nunca** vira query literal), **action** (10 valores documentados em D-078: issued / silenced_no_account / silenced_no_role / silenced_wrong_scope / silenced_no_customer / rate_limited / provider_error / auto_provisioned / verified / verify_failed), **role** (admin/doctor/patient), **IP** (regex allow-list `[0-9a-fA-F:.]+` pra rejeitar injection via query param; Postgres `inet` valida semântica), **intervalo de datas BRT**. Listagem com limit fixo de 200 linhas ordenadas `issued_at DESC`, exibindo só `email_hash` truncado em 8 chars + `…` (hash full no `title` pra copy-paste) e `email_domain` cleartext (métrica operacional, não PII direta — decisão explícita em D-078); IP, UA, route, action, reason ficam visíveis pra triagem forense. 4 cards no topo com contagens das **últimas 24h** independentes dos filtros (total, emitidos = `issued + auto_provisioned`, verificados, incidentes = `rate_limited + provider_error + verify_failed`) — heatmap absoluto pra operador detectar spike (ex: `rate_limited` subir indica enumeração em curso). Seção "Troubleshooting rápido" no rodapé espelha §16 do RUNBOOK com 5 casos comuns. Item "Magic-links" adicionado ao `AdminNav` após "Erros". Read-only por design: sem "reenviar" (reemitir link viola modelo mental de magic-link — só dono do email dispara), sem delete (audit trail imutável por trigger BEFORE DELETE em D-078), sem export CSV (SQL Studio já cobre exportação e evita mover PII/hashes pra arquivo local). Sem testes unitários novos — é server component com queries diretas, `hashEmail()` já coberto em `magic-link-log.test.ts` e filtros em `admin-list-filters.test.ts`. Suite estável em 1440 testes. RUNBOOK §16 reescrito pra priorizar a UI, mantendo SQL como fallback. Artefatos em D-084.
  - PR-073-B/C (D-083): **sweep físico de `appointment_credits` expirados + UI admin dedicada `/admin/credits`** — follow-up técnico do PR-073/D-081. **(B) sweep:** nova rota `/api/internal/cron/expire-appointment-credits` agendada em `0 12 * * *` UTC ≈ 09:00 BRT (horário livre na grade, após admin-digest 11:30 UTC). Lib `sweepExpiredCredits({ limit, now, dryRun })` em `src/lib/appointment-credits.ts` com estratégia SELECT → UPDATE em 2 passos espelhando `asaas-events-retention.ts::purgeAsaasEventsPayload`: SELECT `status='active' AND expires_at <= now ORDER BY expires_at ASC LIMIT ?`, UPDATE com guard `in(ids) AND status='active'` contra concorrência e sobrescrita de `consumed/cancelled`. `dryRun` usa só o SELECT sem efeito colateral. Report estruturado `SweepExpiredCreditsReport` (candidatesFound, expired, errors, errorDetails, oldestExpiredAt, newestExpiredAt) vai pro `cron_runs.payload` pra observability no `/admin/crons`. Limit clampado em `[1, 10_000]` (DEFAULT=500, `MIN_SWEEP_BATCH_LIMIT=1`, `MAX_SWEEP_BATCH_LIMIT=10_000`) contra `?limit=1_000_000` acidental ou `?limit=-1` virar zero silencioso. Por que sweep se o código já tolera sem ele? Relatórios SQL raw (`select count(*) where status='active'`) ficavam mentirosos — compute-on-read vivia só na cabeça de quem implementou. Correção estrutural barata, dívida técnica cara. **(C) UI:** nova página `src/app/admin/(shell)/credits/page.tsx` com duas seções — **Ativos** (cards expandidos, ordenados FIFO por `created_at ASC`, badge terracotta pra `effectiveStatus='active_expired'` "sweep pendente" via `computeCurrentStatus` D-081) e **Histórico** (tabela compacta, filtros canônicos via `admin-list-filters.ts`: busca por nome, status, razão, intervalo BRT). Client component `_Actions.tsx` com 2 modalidades (UUID-form pra consumir, textarea pra cancelar, uma ativa por vez, `router.refresh()` em sucesso). API routes novas: `POST /api/admin/credits/[id]/consume` (body `{ consumed_appointment_id }`, usa `markCreditConsumed`) e `POST /api/admin/credits/[id]/cancel` (body `{ reason }` 4..500 chars, usa `cancelCredit`) — ambas admin-only via `requireAdmin`, ambas auditam em `admin_audit_log` só na transição real (idempotência no-op não polui log, convenção D-033). Integração: item "Créditos" no `AdminNav` logo após "Estornos"; pointer copy em `/admin/reliability` → `/admin/credits`; `admin-inbox.ts::reschedule_credit_pending.href` passa de `/admin/reliability` pra `/admin/credits` (destino operacional real); `EXPECTED_JOBS` e `JOB_LABELS` em `/admin/crons` ganham `expire_appointment_credits`; `CronJob` union em `src/lib/cron-runs.ts` estendido. 7 testes novos em `appointment-credits.test.ts` cobrindo sweep: candidatos vazios, caminho feliz com 3 candidatos, dryRun, erro SELECT, erro UPDATE, parcial (race), clamp de limit. Suite: 1440 testes verdes (era 1433). Por que UI separada de `/admin/reliability`? Bind semântico diferente: reliability é sobre a médica, credit é sobre o paciente — misturar overloadea página já densa. Risco: baixo (cron idempotente, dry-runnable, lib já é compute-on-read; UI nova e isolada sem call-sites externos dependentes). Artefatos em `docs/DECISIONS.md` D-083.
  - PR-074 (D-082): **runbook operacional consolidado + checklist pré-produção** — primeiro PR 100% documental da série, justificado por retorno operacional assimétrico (5-10min economizados por incidente × frequência real × risco do operador solo sob pressão). **(a)** `docs/RUNBOOK.md` revisão cirúrgica 551 → 1014 linhas: tabela de crons na seção 10 reescrita com os **11 jobs reais** do `vercel.json` (inclui `retention-anonymize`, `asaas-events-purge`, `daily-reconcile`, `wa-reminders`, `expire-reservations` que faltavam) corrigindo schedules desatualizados (`recalculate-earnings` é `15 3 * * *` UTC, não `06:00`; `generate-payouts` é `15 9 1 * *`, não `04:00`); seções 15–20 novas cobrindo cada subsistema PR-050..PR-073 (crédito reagendamento, magic-link log, body snapshot, circuit breaker, soft delete CFM, pending_payment ghost) com passos + invariantes + casos excepcionais + queries SQL inline; rotina diária atualizada pra incluir `/admin/crons` (PR-040) + correlação temporal (PR-069) + chip `skipped` (PR-050); seção 5 (payout) ganhou warnings novos de reconciliação de clawback (`clawback_reconciled`, `clawback_dominant_cancelled`, `reconcile_incomplete`); seção 4 (refund) explicita `REFUNDS_VIA_ASAAS` + idempotência do webhook; apêndice de envs agora aponta pro checklist novo como autoridade única (antes tinha lista resumida que induzia "só essas são críticas"). **(b)** `docs/RUNBOOK-PRODUCTION-CHECKLIST.md` criado (0 → 377 linhas) com classificação explícita por criticidade (🔴 bloqueante / 🟠 degradação / 🟡 observabilidade); 8 blocos: bloqueantes legais+regulatórios (PR-023, PR-033-B, DPO, políticas), envs classificadas com referência ao call-site real (`src/lib/X.ts:NN`), catálogo dos 11 crons com SQL de verificação de `cron.job`, feature flags e gates (LEGACY_PURCHASE_ENABLED, REFUNDS_VIA_ASAAS, WHATSAPP_TEMPLATES_APPROVED, state machine D-070), acesso + contas (admin, break-glass PR-047, 2FA PR-038), observabilidade + alarmes, smoke test final de 10min (curl em 4 endpoints + checklist manual no browser), rotação de secrets (cadência 90d, ordem, envs que causam downtime com mitigação por env). **Decisões-chave em D-082:** dois documentos em vez de um (diferença de frequência/formalidade: dia a dia vs. marco), edits cirúrgicas em vez de rewrite (preserva git blame + detalhes operacionais já afiados como procedimento de anonimização na seção 9), sem `RUNBOOK-SQL-RECIPES.md` isolado (queries inline junto do procedimento que as invoca — operador não precisa abrir 2 tabs), datas + versões no rodapé dos 2 docs pra facilitar detecção de drift futuro. **Zero testes, zero código-fonte tocado, zero migration, zero breaking changes.** Risco mitigado de drift futuro: toda seção operacional linka ao ADR/PR que instalou o subsistema (`PR-050 · D-061` etc.), então grep reverso encontra o runbook naturalmente quando aquele ADR for atualizado.
  - PR-073 (D-081): **crédito automático de reagendamento para no-show da médica** — fecha finding [2.4 🟡 MÉDIO]. Migration `20260516000000_appointment_credits.sql` cria a tabela `appointment_credits` (customer_id, source_appointment_id, source_reason in ('no_show_doctor','cancelled_by_admin_expired'), status active/consumed/expired/cancelled, created_at/expires_at, snapshots de consumed_/cancelled_ com email do ator, metadata jsonb). CHECK constraints garantindo coerência `status ⇔ consumed_at ⇔ consumed_appointment_id` e `status='cancelled' ⇔ cancelled_at`, exclusividade terminal (não pode ser consumed+cancelled), reason obrigatório ≥4 chars quando cancelado. Idempotência estrutural via UNIQUE partial `ux_appointment_credits_source_active` em `source_appointment_id WHERE status <> 'cancelled'` — chamadas repetidas de `grantNoShowCredit` (retry, orquestração dupla) viram `alreadyExisted=true`. Trigger `prevent_appointment_credits_source_mutation` BEFORE UPDATE bloqueia mudança de customer_id/source_appointment_id/source_reason/created_at/expires_at (imutáveis). 4 índices direcionados: busca do dashboard do paciente (`customer_id, expires_at desc WHERE status='active'`), watchdog admin-inbox (`created_at asc WHERE status='active'`), cron futuro de expiração (`expires_at asc WHERE status='active'`), auditoria por source appointment. RLS deny-by-default + FORCE, acesso só via service_role. Lib canônica `src/lib/appointment-credits.ts`: (a) `grantNoShowCredit` idempotente via 23505 → re-select, fail-soft; (b) `listActiveCreditsForCustomer` retorna só `active + expires_at > now`; (c) `markCreditConsumed({ actor, consumedAppointmentId, ... })` com guard `status='active'` (idempotência para re-invocação no mesmo appt); (d) `cancelCredit({ reason, actor, ... })` com reason obrigatório ≥4 chars; (e) helpers puros `computeCurrentStatus` (detecta expired on-read), `isCreditActive`, `daysUntilExpiry` defensivo a data inválida. `CREDIT_EXPIRY_DAYS = 90` calibrado pra cobrir férias/feriados sem virar zumbi. Integração em `src/lib/no-show-policy.ts`: nova função privada `emitRescheduleCredit(customerId, sourceAppointmentId, finalStatus)` chamada ao fim dos dois branches (`no_show_doctor` com ou sem payment_id), fail-soft (erro vira `log.warn`, política financeira já aplicada não trava), `NoShowResult.rescheduleCreditId` sobe pra observabilidade. Admin-inbox: nova categoria `reschedule_credit_pending` · `SLA_HOURS.reschedule_credit_pending = 2` (curto porque cada hora é paciente desassistido); conta créditos `active` com `expires_at > now`, idade via `created_at`, CTA leva a `/admin/reliability`; `enqueueEmptyAll` do teste atualizado de 11 pra 12 respostas. UI paciente: novo `RescheduleCreditBanner` no TOPO do `/paciente` (antes de pendingOffers), terracotta-50/200/700 pra diferenciar de ofertas comerciais e de alertas cremes; copy diferenciada por razão (`no_show_doctor` = "Sua próxima consulta é por nossa conta" / `cancelled_by_admin_expired` = "A consulta agendada não aconteceu"); CTA WhatsApp com mensagem pré-preenchida pra o admin reconhecer de cara (sem expor creditId); label de urgência só quando `daysRemaining < 30` ("expira em N dias" / "expira amanhã" / "expira hoje"). `patient-quick-links.ts` ganhou tipo `RescheduleCredit` (discriminated union ready/none), busca paralela em `Promise.allSettled` (sem crescer tempo do dashboard), `toRescheduleCredit` pura devolvendo `none` defensivo quando row já expirou. **Consumo via admin, não self-service** — o paciente clica em "Reagendar pelo WhatsApp", admin agenda a nova consulta e chama `markCreditConsumed`; consistente com modelo solo-operator D-045. Clawback/refund continuam ortogonais ao crédito (se paciente pagou e médica faltou, recebe refund **e** ganha o crédito). **27 testes unitários novos em `appointment-credits.test.ts` (puros: computeCurrentStatus × 5, isCreditActive × 3, daysUntilExpiry × 3; IO com stubs leves: grantNoShowCredit × 5 [validação + feliz + idempotência 23505 + erro não-23505], markCreditConsumed × 5 [validação + feliz + already consumed + not_found + not_active], cancelCredit × 3 [validação + feliz + already cancelled], listActiveCreditsForCustomer × 2, CREDIT_EXPIRY_DAYS × 1) + 4 em `patient-quick-links.test.ts` (toRescheduleCredit × 4: null, ready feliz, expires_at no passado, cancelled_by_admin_expired preservado); suíte 1402 → 1433 (+31).** TSC + ESLint limpos. Sem breaking changes. **Finding [2.4] ✅ RESOLVED — MÉDIOs caem de 3 pra 2 nas PARTES 1+2.** Follow-ups opcionais: **PR-073-B** (cron diário `expire_appointment_credits_sweep` que move active+expirado → expired fisicamente; reuso do índice `ix_appointment_credits_expiry_sweep`) e **PR-073-C** (UI dedicada `/admin/credits` pra listar/consumir/cancelar explicitamente sem depender de `/admin/reliability`).
  - PR-072 (D-080): **self-service do paciente — atalhos no dashboard + preços sob demanda em `/renovar`** — fecha findings [1.7 🟡 MÉDIO] e [1.6 🟡 MÉDIO] num único PR UX adjacente, zero schema, zero breaking changes. **(a) [1.7]** Lib pura `src/lib/patient-quick-links.ts` com `getPatientQuickLinks(supabase, customerId)` disparando duas queries via `Promise.allSettled` (prescrição imutável de `appointments.memed_prescription_url` via PR-030; endereço em `customers.address_*`); helpers puros `toLatestPrescription`/`toShippingAddress` retornam discriminated unions (`ready`/`none`/`incomplete`/`missing`) pra forçar exaustividade no render; validação defensiva de `javascript:` vs http(s), normalização `trim`, fallback `"Médica"` pra display. `QuickLinksSection` nova entre "Próxima consulta" e "Consultas recentes" no `/paciente` só renderiza se pelo menos um atalho tem conteúdo (paciente novo não vê card vazio). Fail-soft total: erro de banco → `log.error` + kind `none`/`missing`, dashboard continua renderizando. **(b) [1.6]** Preços em `/paciente/renovar` embalados em `<details>` HTML nativo (zero JS custom, zero client component, acessibilidade keyboard/screen reader gratuita); `<summary>` "Ver valores de referência (N plano(s))" com indicador "clique pra expandir/recolher" via `group-open:hidden`/`hidden group-open:inline` Tailwind. "ou R$ 750 em cartão" substituído por "parcelamento disponível após a reconsulta" (âncora numérica removida). Copy acima do toggle reforça "os valores são referência: o preço final pode variar conforme a médica ajustar a dose ou trocar de plano" + nota adicional dentro do toggle. `plan.medication` removido da renderização (consistente com reduzir âncora clínica + ansiedade financeira). **Limitação conhecida**: farmácias parceiras (item c do finding [1.7]) vira PR-072-B quando houver parceria comercial estabelecida. **32 testes novos na lib pura (pickIssuedAt × 4, extractDoctorName × 7, toLatestPrescription × 10, toShippingAddress × 11); suíte 1370 → 1402. Tripwire `public-pages-safety.test.ts` continua passando (`/renovar` é rota autenticada).** **Findings [1.6] e [1.7] ✅ RESOLVED — MÉDIOs caem de 5 pra 3 nas PARTES 1+2.** Zero breaking changes.
  - PR-071 (D-079): **deprecação suave de `appointments.status='pending_payment'` resíduo do D-044** — fecha finding [1.4 🟡 MÉDIO]. Enum permanece (remover quebraria RPC legada `book_pending_appointment_slot`, state machine D-070 e linhas históricas), mas três peças complementares endereçam o UX ghost: (a) **migration `20260515000000_pending_payment_deprecation.sql`** adiciona `COMMENT ON COLUMN appointments.status` e `pending_payment_expires_at` marcando textualmente o valor como LEGADO D-044 + índice parcial `idx_appointments_pending_payment_legacy(pending_payment_expires_at asc) WHERE status='pending_payment'` (custo de manutenção ~0 porque em produção estável com `LEGACY_PURCHASE_ENABLED=false` a lista tende a 0 linhas); (b) **`src/app/paciente/(shell)/page.tsx`** — card `isPendingPayment` ganha CTA "Fale com a equipe pelo WhatsApp" via `whatsappSupportUrl(...)` da lib `contact.ts` (PR-057 · D-068) com mensagem pré-preenchida pra acelerar triagem, e layout revisado (caixa branca, copy mais orientada a caminho humano em vez de passivo); (c) **`src/lib/admin-inbox.ts`** — nova `InboxCategory='appointment_pending_payment_stale'` com `SLA_HOURS=24h` (conforme sugestão explícita do finding item (c)); aparece em `/admin` home com link pra `/admin/health`; usa `appointments.created_at` como proxy de idade (não `pending_payment_expires_at`, que só vai 15min à frente e não reflete "ghost há muito"). **Invariante**: em produção com `LEGACY_PURCHASE_ENABLED=false` (default desde PR-020 · D-048) nenhum novo appointment `pending_payment` é criado — se o watchdog dispara, é ghost/bug sério, admin triage manualmente (não auto-cancelamos pra evitar duplo-estorno). **Trade-offs documentados em D-079:** proxy `created_at` (não `expires_at`), SLA 24h (sinalizar sem alarmar), sem auto-cancelamento (segurança > conveniência), sem remoção do enum (período mínimo 180d consecutivos com LEGACY=false antes disso). **Testes:** `src/lib/admin-inbox.test.ts` +3 casos (overdue 36h, <50% SLA descarta, SLA=24h); `enqueueEmptyAll` atualizado de 9 pra 11 respostas (contempla também lgpd_requests que já existia + o novo pending_payment). Suíte global 1367 → **1370** (+3). TSC + ESLint limpos. **Finding [1.4] ✅ RESOLVED — MÉDIOs caem de 6 pra 5 na PARTES 1+2.** Zero breaking changes; zero risco em produção.
  - PR-070 (D-078): **trilha forense de emissões e verificações de magic-link em `magic_link_issued_log` com política LGPD-safe** — fecha finding [17.8 🟡 MÉDIO]. Migration `20260514000000_magic_link_issued_log.sql` cria tabela imutável com `email_hash text not null check (hex 64)` (SHA-256 determinístico de `email.trim().toLowerCase()`, reproduz busca "alice recebeu link?" sem armazenar email plaintext em disco), `email_domain text check (char_length <= 253)` (métrica de provedor sem PII direta), `role text` (admin/doctor/patient/null), `action text not null` com 10 estados (`issued`, `silenced_no_account`, `silenced_no_role`, `silenced_wrong_scope`, `silenced_no_customer`, `rate_limited`, `provider_error`, `auto_provisioned`, `verified`, `verify_failed`), `reason text (≤500)`, `route text (≤200)`, `ip inet`, `user_agent text (≤500)`, `next_path text`, `metadata jsonb default '{}'`, `issued_at timestamptz`. 4 índices forenses (`(email_hash, issued_at desc)` para triagem, `(action, issued_at desc)` para métricas, `(ip, issued_at desc) where ip is not null` para detecção de abuso, `(issued_at desc)` para rolagem global). Imutabilidade: triggers `prevent_magic_link_mutation` em UPDATE e DELETE com bypass controlado via GUC `app.magic_link_log.allow_mutation` (só DBA em transação interativa); helper `magic_link_log_mutation_allowed()` stable lê a GUC (missing_ok). RLS deny-by-default + FORCE, zero policies — acesso só via `service_role`. Lib canônica `src/lib/magic-link-log.ts`: (a) `hashEmail(email)` lança em vazio/não-string, SHA-256 hex 64 chars, normaliza case/trim; (b) `extractEmailDomain(email)` lowercase + trunca 253 (FQDN limit), null em malformado, pega o último `@` como delimitador; (c) `buildMagicLinkContext(req, route)` extrai IP via `x-forwarded-for`[0] → `x-real-ip`, trunca UA em 500; (d) `logMagicLinkEvent(supabase, input)` fail-soft: nunca lança, retorna discriminated union `{ok:true,id}`/`{ok:false,insert_failed}`/`{ok:false,missing_email}`. Aceita `email=null` apenas em `verify_failed` e `rate_limited` (callers realmente podem não ter acesso ao email nesses states); outras actions sem email → `missing_email` sem inserir (bug do caller). Para email garbage que quebra hashEmail (ex: string só whitespace) em actions que permitem null, usa hash fallback determinístico `SHA-256("unknown:<action>:<iso-minute>")` — preserva formato do CHECK regex e permite agrupar rajadas temporais sem granularidade individual (trade-off documentado). Trunca defensivamente `reason` (500), `route` (200), `next_path` (500), strings em `metadata` (2048, sufixo `…[truncated]`); omite `undefined`. Integração em 3 endpoints: (a) `POST /api/auth/magic-link` loga `rate_limited` → `provider_error (listUsers)` → `silenced_no_account` → `silenced_no_role (reason=role=X)` → `provider_error (signInWithOtp)` → `issued`; (b) `POST /api/paciente/auth/magic-link` inclui `silenced_no_customer` (antes de tocar auth.users), `silenced_wrong_scope` (role admin/doctor não-deixa logar como paciente), `auto_provisioned` (cria auth.user com role=patient antes de emitir, com `metadata.customer_id`), `issued` com `metadata.auto_provisioned: boolean`; (c) `GET /api/auth/callback` loga `verify_failed` (type inválido / verifyOtp erro / exchangeCodeForSession erro / sem params) ou `verified` (extrai email + role de `data.user.app_metadata`). Callback usa cliente admin separado (`getSupabaseAdmin()`) pra log — não reusa `getSupabaseRouteHandler()` que escreve cookies de sessão, evita acoplamento conceitual. Todas as chamadas são `void logMagicLinkEvent(...)` — fail-soft explícito, erro no log nunca bloqueia resposta ao usuário (privar de receber link porque audit offline é pior que perder uma linha). **Triagem de "não recebi o link" vira 1 query:** `select action, issued_at, reason, ip from magic_link_issued_log where email_hash = sha256('alice@yahoo.com.br') order by issued_at desc`. Detecção de abuso ganha superfície: `rate_limited` + `silenced_*` agrupados por IP revela enumeração. **32 testes novos** (hashEmail determinismo × 2, case/trim × 3, distinção × 1, empty→throw × 3, tipo errado × 2; extractEmailDomain × 7; buildMagicLinkContext × 4; logMagicLinkEvent × 12 cobrindo happy path, email opcional em verify_failed/rate_limited, missing_email em outras actions, fail-soft em INSERT error, truncamento de reason/route/next_path, sanitização de metadata, hash equivalente em emails case-trim-equivalentes, fallback hash pra email garbage, metadata vazia default, exception síncrona). Suíte global 1335 → 1367. Zero breaking changes no fluxo de login/callback. **Limitações conhecidas documentadas:** UI `/admin/magic-links` não criada (consulta via SQL editor ou script ad-hoc; UI fica como PR-070-B opcional). Integração com bounce/complaint webhook de Resend/Postmark continua residual — requer decisão operacional sobre provedor SMTP (PR-070-C quando volume justificar). **Finding [17.8] ✅ RESOLVED — MÉDIOs caem de 6 pra 5 na PARTE 4.**
  - PR-069 (D-077): **correlação temporal entre falhas de cron e demais fontes de erro, sem tabela física nova** — fecha finding [17.5 🟡 MÉDIO]. Abordagem escolhida: *computed view* on-demand, rejeitando a sugestão original de tabela física `error_log` unificada (duplicaria dados, já que `error-log.ts` D-045 · 3.G já é a view lógica das 5 fontes cron/asaas/daily/notification/whatsapp). Lib pura `src/lib/cron-correlation.ts`: `correlateErrorsInWindow(entries, { anchorAt, windowMinutes, excludeReference? })` filtra `ErrorEntry[]` pra `[anchor ± N min]`, ordena por proximidade ao anchor (empates por `occurredAt` desc), exclui própria referência do cron (formato `cron_runs:{uuid}`), clamp em [1, 1440] min (default 15), fail-safe a datas inválidas. `clampWindowMinutes`, `formatCorrelationSummary` (ordem determinística cron → Asaas → Daily → envio WA → entrega WA; omite zeros). Orquestrador em `cron-dashboard.ts`: `loadCronDashboard(..., { correlation: true, correlationWindowMinutes: 15 })` chama **UMA** query ao error-log cobrindo a janela do dashboard (evita N+1 quando múltiplos jobs falharam) e popula `CronJobSummary.last_error_correlation = { window_minutes, total, by_source, top_entries[≤5] }`. Fail-soft: se `loadErrorLog` falhar, cada job fica com `null` + `log.error` estruturado, dashboard continua renderizando. `correlation` é opt-in (default `false` → zero impacto em callers legados). UI `/admin/crons` dentro do bloco "Último erro": `<CorrelationInline>` renderiza "± 15min: 2 Asaas · 1 envio WA. **ver correlação →**" (link pra `/admin/errors?ts={last_error_at}&w=15`) quando `total > 0`, ou "± 15min: sem outros erros. *Provável bug deste cron, não dependência externa.*" quando `total == 0` — ambos diagnósticos valiosos. UI `/admin/errors` ganha params `?ts=ISO&w=minutos`: amplia auto-janela do loader pra cobrir 2×raio, filtra via `correlateErrorsInWindow`, banner terracotta "Modo correlação: ±Nmin em torno de DD/MM HH:MM" + botão "limpar filtro"; filtros existentes (`?h=`, `?source=`) preservam `ts`/`w`. **Sem migration, sem breaking changes, sem alteração em API de `loadCronDashboard` pra callers existentes.** **20 testes novos cobrindo a lib pura (clampWindowMinutes × 4, correlateErrorsInWindow × 11 [vazio, anchor inválido, janela, ordenação/empates, exclude, exclude null, datas inválidas, clamp, Date anchor, sinceIso coerente, não-muta, multi-source], formatCorrelationSummary × 4); suíte global 1315 → 1335.** **Finding [17.5] ✅ RESOLVED — MÉDIOs caem de 7 pra 6 na PARTE 4.**
  - PR-068 (D-076): **log granular de confiabilidade do paciente** — fecha finding [17.6 🟡 MÉDIO]. Migration `20260513000000_patient_reliability_events.sql` cria `patient_reliability_events` espelhando schema de `doctor_reliability_events` (D-036) com 5 kinds (`no_show_patient`, `reservation_abandoned`, `late_cancel_patient`, `refund_requested`, `manual`), RLS admin-only (`pre_admin_only`), índices `ix_patient_reliability_active_recent` (lookup "últimos 90d ativos") + `ux_patient_reliability_appt_kind` partial unique em (appointment_id, kind) para idempotência estrutural + `ix_patient_reliability_kind_active`. Trigger DB `trg_record_patient_reliability` AFTER UPDATE OF status ON appointments registra automaticamente os 3 kinds automáticos: `no_show_patient` direto, `reservation_abandoned` via `cancelled_by_admin + cancelled_reason='pending_payment_expired'`, `late_cancel_patient` via `cancelled_by_patient` com `scheduled_at - now() < 2h`. **Desacoplado de código TS** — qualquer caller (reconcile, pg_cron `expire_abandoned_reservations`, webhook, UI futura de cancel) dispara o registro automaticamente. Fail-safe: `exception when others` na trigger vira `RAISE NOTICE` sem derrubar o UPDATE principal — trigger de observabilidade nunca pode quebrar fluxo de negócio. Lib canônica `src/lib/patient-reliability.ts`: (1) `recordManualEvent` com allowlist `MANUAL_KINDS = ['manual', 'refund_requested']` (kinds automáticos proibidos via trigger — evita duplicata); valida UUID customer, kind allowlist, notes mínimo 4 chars após sanitize de controles; idempotência via 23505 em (appointment_id, kind). (2) `dismissEvent` idempotente (already-dismissed = no-op), valida reason ≥4 chars. (3) `getPatientReliabilitySnapshot` — janela **90 dias** (vs. 30d da médica, porque paciente tem frequência menor), soft-warn=2, hard-flag=3, breakdown por kind, lastEventAt; retorna null em UUID inválido / customer inexistente / erro DB. (4) `listCustomerEvents`, `listRecentEvents` (com join customers/appointments). (5) `computeSnapshotFromEvents` pura pra teste unitário. Diferença-chave vs. médica (D-036): **sem auto-block de paciente no MVP** — apenas sinalização. Risco de falso positivo (paciente cancelou por doença, volta depois) > benefício marginal até ter 3+ meses de dados pra calibrar; decisão reversível em PR-068-B. UI: seção "Confiabilidade" em `/admin/pacientes/[id]` (`_ReliabilityBlock.tsx`) com cartão de status (verde/ambar/terracotta), breakdown por kind, histórico dos últimos 20 eventos diferenciando ativos de dispensados. Ações interativas (dispensar / registrar manual via botão) ficam pra PR-068-B (precisa API routes + fluxo de auditoria). **30 testes novos: computeSnapshotFromEvents (vazio, fora-janela, breakdown, soft/hard thresholds, lastEventAt, data inválida) × 7; recordManualEvent (validação UUID/kind/notes, happy path, appointmentId opcional, idempotência 23505, db_error) × 9; dismissEvent (validação, not_found, alreadyDismissed, happy) × 5; getPatientReliabilitySnapshot (null em UUID inválido/customer inexistente/erro events; snapshot agregado correto) × 4; listCustomerEvents/listRecentEvents (validações, clamp limit 200, contract com joins, erro DB) × 5. Suíte global 1285 → 1315.** Sem breaking changes, sem alteração em `no-show-policy.ts` ou `reconcile.ts` (trigger DB cobre tudo). **Finding [17.6] ✅ RESOLVED — MÉDIOs caem de 8 pra 7 na PARTE 4.**
  - PR-067 (D-075): **snapshot forense do body + target_phone em `appointment_notifications`** — fecha finding [17.7 🟡 MÉDIO]. Migration `20260512000000_appointment_notifications_body_snapshot.sql` adiciona `body text`, `target_phone text`, `rendered_at timestamptz` em `appointment_notifications` (todas nullable pra compatibilidade com histórico). Trigger `trg_an_body_immutable_after_send` BEFORE UPDATE bloqueia alteração de `body`/`target_phone`/`rendered_at` **e** bloqueia zerar `sent_at` uma vez que `sent_at` foi preenchido (evidência jurídica CFM 2.314/2022 + CDC Art. 39 VIII imutável). Antes de `sent_at` ser preenchido, permite reescrita — crítico pra suportar retry que re-renderize após paciente atualizar telefone (PR-056). Índice parcial forense `idx_an_target_phone_sent(target_phone, sent_at desc) where target_phone is not null and sent_at is not null` otimiza consulta "me mostra tudo que foi enviado pro número X, mais recente primeiro". Lib canônica `src/lib/appointment-notifications.ts`: (1) `renderNotificationBody(kind, ctx): {body, templateName, targetPhone}` PURA — cobre os 10 kinds (`confirmacao`, `t_minus_24h/1h/15min`, `on_demand_call`, `pos_consulta`, `t_plus_10min`, `reserva_expirada`, `no_show_patient/doctor`) espelhando os templates Meta 1:1 com `replaceVars` simples sem regex complexa + exaustividade estática via `never`; (2) `recordBodySnapshot(supabase, {notificationId, body, targetPhone, now?})` idempotente — UPDATE guardado por `.is("sent_at", null)` respeita o trigger, retorna distinção `{updated, alreadySent}`/`not_found` via SELECT de fallback, validação defensiva (id ≥ 8 chars, body non-empty post-trim, phone truncado 32 chars, body truncado 8000 chars); (3) `maskPhoneForAdmin(phone, {visible?=4})` — DDI+DDD visíveis em telefones ≥12 dígitos, fallback masked-only em telefones curtos, `—` em null, `****` em inválido, clamp `visible ∈ [0,6]` (bug `slice(-0)` resolvido explicitamente); (4) helpers expostos `replaceVars` e `normalizePhoneDigits` pra testabilidade. Integração em `src/lib/notifications.ts::processDuePending` via `snapshotBodyForRow()` chamada **antes** de `dispatch()` (body + phone ficam persistidos mesmo se HTTP pra Meta falhar — evidência do que *seria* enviado, não só do que foi). Falha do snapshot é WARN (não-fatal) — não bloqueia dispatch. UI `/admin/notifications` ganha coluna "Conteúdo" com telefone mascarado + `<details>` colapsável do body (admin expande caso-a-caso, não expõe PII em massa na listagem). **49 testes novos (replaceVars × 5, normalizePhoneDigits × 4, maskPhoneForAdmin × 7, renderNotificationBody × 22 cobrindo todos os kinds + exaustividade + fallbacks de payload + normalização de telefone, recordBodySnapshot × 11 cobrindo validação de input + happy path + idempotência (already-sent, not-found, db_error); suíte global 1236 → 1285.** Sem breaking changes, sem mudança de RLS (deny-by-default `an_admin_only` já cobre). **Finding [17.7] ✅ RESOLVED — MÉDIOs caem de 9 pra 8 na PARTE 4.**
  - PR-066 (D-074): **soft delete para prontuário e audit financeiro** — fecha finding [10.8 🟡 MÉDIO]. Migration `20260511000000_soft_delete_clinical_tables.sql` adiciona 4 colunas (`deleted_at/deleted_by/deleted_by_email/deleted_reason`) nas 4 tabelas CFM-core (`appointments`, `fulfillments`, `doctor_earnings`, `doctor_payouts`). Trigger `prevent_hard_delete_<table>` BEFORE DELETE em cada tabela levanta `raise exception` bloqueando `DELETE` acidental (admin solo no SQL Studio, migration com `TRUNCATE`, cron buggy, hotfix mal pensado). Bypass explícito apenas via DBA: `SET LOCAL app.soft_delete.allow_hard_delete='true'` numa transação — helper stable `soft_delete_hard_delete_allowed()` lê a GUC (missing_ok) dentro da trigger. Trigger `enforce_soft_delete_fields` BEFORE UPDATE OF deleted_at/deleted_reason exige `deleted_reason` não vazio quando `deleted_at` transita null → not null. CHECK constraint `*_soft_delete_reason_chk` garante a invariante em disco. 8 índices parciais `WHERE deleted_at IS NULL` cobrem padrões frequentes (doctor+scheduled, customer+scheduled, doctor+status). Lib canônica `src/lib/soft-delete.ts` com `softDelete(supabase, { table, id, reason, actor, now? })`: whitelist de tabelas (`SOFT_DELETE_TABLES`), validação defensiva (reason ≥4 chars após trim, remove control chars, trunca 500), idempotência (já deletado → `alreadyDeleted: true`), race handling (`UPDATE ... WHERE deleted_at IS NULL` sem row → re-read), integração D-072 (actor snapshot normalizado, `kind=system` força `userId=null`). Tabelas já imutáveis (`plan_acceptances`, `admin_audit_log`, `patient_access_log`, `document_access_log`, `checkout_consents`, `appointment_state_transition_log`) continuam cobertas pelos respectivos triggers de imutabilidade — fora do escopo. **18 testes novos; suíte 1218 → 1236.** Nenhum call-site do app alterado (grep confirma zero `.delete()` nas 4 tabelas). Quando primeira UI de soft delete for implementada, `addActiveFilter(q)` ou `.is("deleted_at", null)` explícito nos SELECTs relevantes (PR-066-B reativo, sem trabalho proativo). Onda B (outras tabelas: `customers`/`leads` cobertos por anonimização própria D-051/D-052; `doctor_billing_documents`/`doctor_payment_methods`/`doctor_availability` têm `.delete()` legítimo, não são prontuário) explicitamente diferida. **Finding [10.8] ✅ RESOLVED — MÉDIOs caem de 9 pra 8 na PARTE 4.**
  - PR-065 (D-073): **limpezas MÉDIAs · tripwire CFM em páginas públicas + copy de repasse** — fecha findings [7.5 🟡], [7.6 🟡] e [2.5 🟡]. (a) **[7.5]** confirmado RESOLVED retroativamente pelo PR-020 (gate `isLegacyPurchaseEnabled()` já redireciona `/checkout/[plano]` e `/agendar/[plano]` em produção com default `false`). (b) **[7.6]** corrige exposição concreta: `/planos` renderizava `plan.medication` do DB (potencialmente "Tirzepatida"/"Semaglutida") sem autenticação — removido do `select()` e do JSX; continua disponível nas rotas autenticadas. Tripwire permanente em `src/app/public-pages-safety.test.ts`: varre fontes de TODAS páginas não-autenticadas (home, sobre, planos, termos, privacidade, checkout/agendar legados, logins) + `src/components/*.tsx` e **falha o build** se encontrar nome comercial (Ozempic, Mounjaro, Wegovy, Rybelsus, Saxenda, Victoza, Trulicity, Byetta, Bydureon) ou princípio ativo (Tirzepatida, Semaglutida, Liraglutida, Dulaglutida, Exenatida) em word boundary case-insensitive. "GLP-1" permitido (classe terapêutica em contexto regulatório citando Nota Técnica Anvisa 200/2025 em `/sobre` e `/termos` — legalmente defensável). (c) **[2.5]** remove o `+` enganoso do hint "Recebido neste mês" (induzia médica a somar mentalmente valor confirmado com pendentes que podem cair só em M+1). Lib pura `src/lib/doctor-dashboard-copy.ts` com `formatReceivedThisMonthHint` ("N repasse(s) aguardando confirmação") e `formatPendingConfirmationNote` ("Você tem N repasse(s) em andamento. Esse valor pode/podem cair neste mês ou no próximo, conforme confirmação bancária."). `draft` excluído de "awaiting" (pre-approval). **15 testes novos** (3 tripwire + 12 copy); suíte 1218/1218. Sem migration, sem breaking changes. **MÉDIOs caem de 9 pra 6 na tabela das PARTES 1+2.**
  - PR-064 (D-072): **snapshot de identidade do ator em campos de audit (onda A)** — fecha finding [10.6 🟡 MÉDIO] pra as 4 colunas mais sensíveis CFM/LGPD/financeiro. Estratégia "Ghost user" pareada com FK (não `restrict` como a auditoria sugeriu — conflitaria com LGPD Art. 18). Migration `20260510000000_actor_audit_snapshots.sql` adiciona `plan_acceptances.user_email` (prova legal imutável; backfill desabilita temporariamente o trigger `trg_plan_acceptances_immutable`), `fulfillments.updated_by_email`, `appointments.refund_processed_by_email`, `doctor_payouts.approved_by_email` — todas com backfill via JOIN com `auth.users`. Libs novas: `src/lib/actor-snapshot.ts` (normalização `trim+lowercase+empty→null`, `ActorKind` union, `systemActorSnapshot("<label>")` pra crons/webhooks) e `src/lib/user-retention.ts` (helper `anonymizeUserAccount` que substitui email por `anon-<hash12>@deleted.local` determinístico, bana login via `ban_duration: 876000h`, preserva UUID pro D-072 — disponível mas não acionado por cron ainda; PR-064-C futuro terá UI admin). Call-sites atualizados pra propagar email: `acceptFulfillment(userEmail)`, `transitionFulfillment(actorEmail)`, `updateFulfillmentShipping(actorEmail)`, `finalizeAppointment(userEmail)`, `markRefundProcessed(processedByEmail)`, `processRefundViaAsaas(processedByEmail)`, `/api/admin/payouts/[id]/approve` (insere `approved_by_email`). Webhook Asaas passa `"system:asaas-webhook"` no refund automático. **33 testes novos (20 actor-snapshot + 10 user-retention + 5 nos testes existentes de fulfillment-transitions/acceptance/refunds/shipping/finalize); suíte 1204/1204.** Onda B (doctor_billing_documents, reliability_paused_by, dismissed_by, replaced_by, plans.created_by, lgpd_requests, appointments.{created_by,cancelled_by_user_id}) permanece como **[10.6-B]** backlog — baixo volume, defer até uso concreto. **Finding [10.6] ✅ RESOLVED — MÉDIOs caem de 10 pra 9.**
  - PR-061 (D-071): **schemas defensivos pra colunas JSONB app-geradas** — fecha parcialmente finding [10.4 🟡 MÉDIO]. Lib `src/lib/jsonb-schemas.ts` zero-dep com dois níveis: (1) `validateSafeJsonbValue`/`validateSafeJsonbObject` genéricos que rejeitam undefined/NaN/Infinity/bigint/função/símbolo, instâncias não-literais (Date/Error/Map/Set/Promise/RegExp), refs circulares, chaves `__proto__`/`constructor`/`prototype`, profundidade > 6, strings > 4 KiB, serialização > 16 KiB; cópia defensiva no retorno; (2) schemas estritos `validateShippingSnapshot` (CEP 8 dígitos, UF 2 letras maiúsculas, tamanhos por campo) e `validateAddressChangeSnapshot` (versão prefixo `shipping_*` com todos campos opcionalmente null, tolerância undefined→null e chaves extras). Integração: `cron-runs.finishCronRun` em **fail-soft** (stub rastreável `{ _validation_failed: true, _job, _issue_count, _first_issue }` + `log.warn` — preserva registro do run); `patient-update-shipping` e `fulfillment-acceptance` em **fail-hard** (abortam `db_error` com `log.error` — snapshot inválido é regressão em `patient-address.ts`, não input do usuário). **36 testes novos; suíte 1169/1169.** Sem migration, sem breaking changes. Webhooks externos (`asaas_events.payload`, `daily_raw`, `whatsapp_events.payload`) ficam fora do escopo deliberadamente (espelho do provider, schema pode mudar sem aviso) — virou **[10.4-B]** pendente, addressed no PR-061-B futuro via contract tests + views tipadas + alerta pg_column_size > 100 KB. **Finding [10.4] parcialmente resolvido — superfície app-gerada fechada.**
  - PR-059 (D-070): **state machine declarativa de `appointments.status` via trigger DB** — fecha finding [10.5 🟡 MÉDIO]. Migration `20260509000000_appointment_state_machine.sql` cria (a) tabela `appointment_state_transitions(from_status, to_status, description)` seedada com **as 28 transições reais** mapeadas via grep do código em 2026-04-20 (`pending_payment → {scheduled, cancelled_*, completed defensivo, no_show_*}`, `scheduled → {confirmed, in_progress, completed, no_show_*, cancelled_*}`, `confirmed → mesmas saídas`, `in_progress → {completed, no_show_*, cancelled_*}`); (b) tabela imutável `appointment_state_transition_log` com triggers BEFORE UPDATE/DELETE bloqueando edição + RLS deny-all; (c) trigger `validate_appointment_transition` BEFORE UPDATE OF status com modo controlado por GUC `app.appointment_state_machine.mode ∈ {warn, enforce, off}` (default **`'warn'`** — registra mas não bloqueia, pra coletar evidência); (d) bypass por transação via `SET LOCAL app.appointment_state_machine.bypass='true'` + `bypass_reason` (sempre logado). Lib espelho `src/lib/appointment-transitions.ts` exporta `isAllowedAppointmentTransition`, `isTerminalAppointmentStatus`, `listForbiddenTransitionsFrom`. **13 testes novos cobrindo invariantes (sem duplicata, sem self-loop, terminal nunca aparece como `from`, transições reais do código permitidas, transições impossíveis bloqueadas) + helpers; suíte 1133/1133.** Plano de promoção a `enforce`: observar `appointment_state_transition_log` por 1-2 semanas, quando 7 dias seguidos sem warning rodar `ALTER DATABASE postgres SET app.appointment_state_machine.mode='enforce'`. Risco operacional zero no deploy (modo warn). **Finding [10.5] ✅ RESOLVED — MÉDIOs caem de 10 pra 9 na PARTE 1+2.**
  - PR-058 (D-069): **filtros + busca em `/admin/fulfillments`, `/admin/payouts`, `/admin/refunds`** — lib pura `src/lib/admin-list-filters.ts` com `parseSearch` (max 80 chars), `parseStatusFilter<T>` (allowlist tipada), `parseDateRange` (BRT→ISO UTC, valida 31 fev / ano fora de [2020, 2100], sinaliza inversão), `parsePeriodFilter` (YYYY-MM), `escapeIlike`/`escapeOrValue` (mesmas convenções do `patient-search.ts`), `buildAdminListUrl`/`hasActiveFilters`. Cada página ganhou `FilterBar` server-form (`method=get`, sem JS, URL canônica bookmarkable) com modo dual: sem filtro mantém UX original (grupos por status); com filtro vira tabela única `created_at desc` (limit 200). Search por nome de médica/paciente usa sub-query (`doctors`/`customers` resolvidos primeiro → `id IN (...)`) pra evitar PostgREST `.or()` em coluna relacionada. **40 testes novos; suíte 1120/1120.** Sem migration. **Finding [8.7] ✅ RESOLVED — MÉDIOs caem de 10 pra 9 na PARTE 1+2.**
  - PR-057 (D-068): **Onda 3A · MÉDIOs (1.5 + 8.5; doc-only 10.7 + 8.6)** — `src/lib/contact.ts` centraliza canal público (`NEXT_PUBLIC_WA_SUPPORT_NUMBER` + `NEXT_PUBLIC_DPO_EMAIL` com fallbacks defensivos; helpers `whatsappSupportUrl/getSupportWhatsappE164/telSupportUrl/getDpoEmail`; sanitiza máscaras; valida 10–13 dígitos). `/paciente/renovar` migrado pra usar o helper. `src/lib/dashboard-health.ts::evaluateUnknownSourceRatio` puro (threshold 5%, mínimo 20 amostras pra evitar volatilidade); chip terracotta no `/admin` quando alerta. `[10.7] cpf UNIQUE` confirmado por leitura (`migrations/20260419030000_asaas_payments.sql:117` declara `cpf text not null unique`); auditoria pediu confirmação, era falso positivo. `[8.6] last_run dos crons` 100% endereçado pelo PR-040 (sparklines + percentis + delta semana + jobs em atraso). **22 testes novos (15 contact + 7 dashboard-health); suíte 1080/1080.** Sem migration. **Findings [1.5], [8.5], [8.6], [10.7] ✅ RESOLVED — MÉDIOs caem de 12+12 pra 11+11 nas tabelas das PARTES 1+2 e PARTE 3.**
  - PR-056 (D-067): **self-service de atualização de PII em `/paciente/meus-dados/atualizar`** — destrava a fricção criada pelo guard D-065 (paciente com `customer.user_id` não tinha caminho pra atualizar e-mail/phone/endereço fora do funil de compra, que agora bloqueia silenciosamente). POST `/api/paciente/meus-dados/atualizar` protegido por `requirePatient()`; lib pura `src/lib/meus-dados-update.ts` (`parseAndValidateUpdate` + `computeChangedFields`) reusa `sanitizeShortText(personName)` (PR-037), `validateAddress` (PR-035), regex de e-mail + dígito-only de telefone; CPF silenciosamente ignorado se vier no payload (evita oracle); `anonymized_at` responde 409; diff normalizado (case/lower/digits/complement null-vs-empty) evita UPDATE + log de não-evento; trilha LGPD reusa action `pii_updated_authenticated` (D-065) com `actor_kind='system'` + `metadata.self_service=true` + `changed_fields[]`. UI em `src/app/paciente/(shell)/meus-dados/atualizar/page.tsx` (server) + `_AtualizarForm.tsx` (client com ViaCEP via proxy `/api/cep/[cep]` + máscaras). Link "Atualizar dados" no topo do `/paciente/meus-dados`. **27 testes novos; suíte 1058/1058.** Sem migration, sem breaking changes. Follow-ups opcionais: **PR-056-B** (sync Asaas `updateCustomer`) e **PR-056-C** (OTP antes de trocar e-mail principal).
  - Onda 1A (D-047): PR-024, PR-025, PR-026 (dark patterns + fail-fast CRON_SECRET)
  - Onda 1B (D-048): PR-013, PR-020, PR-030, PR-031 (integridade financeira, prontuário, audit log)
  - Onda 1C (D-049): PR-011, PR-015, PR-021 (acceptance server-side, race em payment, timezone BR)
  - Onda 1D (D-050): PR-014 (earning financeiro só em PAYMENT_RECEIVED)
  - Onda 2A (D-051): PR-016 (allowlist em export LGPD), PR-017 (self-service /paciente/meus-dados), PR-032 (patient_access_log + logPatientAccess integrado em view, export, anonymize, search, lgpd_fulfill, lgpd_reject)
  - Onda 2B (D-052): PR-033-A (retenção LGPD automática — cron semanal anonimiza ghost customers > 24 meses; actor_kind formalizado em admin_audit_log + patient_access_log; bug do `patient_access_log.admin_user_id NOT NULL` corrigido; seção informativa em /admin/lgpd-requests; freshness check em /admin/health)
  - Onda 2C (D-053): PR-035 (ViaCEP blindado) — proxy server-side `/api/cep/[cep]` com schema estrito + charset allowlist + rate-limit + cache de borda; `validateAddress` server-side ganha `hasControlChars` + regex de charset em todos os campos; clients migrados pra consumir o proxy. Fecha audit [22.1] e prepara [9.1] (agentes LLM).
  - Onda 2D (D-054): **PR-036 (`/api/lead` endurecido)** — lib `text-sanitize` (compartilhada) + `lead-validate` (charset slug em `answers`, limites em nome/phone/utm/referrer/landingPath, 37 testes) + rate-limit 10/15min por IP + body-guard 8 KB pré-parse + migration com CHECK constraints em `pg_column_size(answers/utm)` e `char_length(name/phone/status_notes/referrer/landing_path)` + índice parcial `leads_ip_created_at_idx` pra futuro cron de anti-spike. Fecha audit [9.3] e [22.2]; mitiga [9.1] em `leads.answers`.
  - Onda 2E (D-055): **PR-036-B (clínico/operacional endurecido)** — `hasEvilControlChars` + `cleanFreeText` + `sanitizeFreeText` em `text-sanitize` (aceita `\n\r\t`, bloqueia NULL/ESC/DEL/zero-width/bidi/U+2028-29); aplicado em `appointment-finalize.ts` (hipotese/conduta/anamnese.text com limites 4 KB/16 KB) e `fulfillment-transitions.ts` (tracking_note 500ch / cancelled_reason 2 KB); `validateFinalizeInput` passa a devolver `{ ok, sanitized }`; migration `20260503000000` com CHECK em `appointments.hipotese/conduta/anamnese` + `fulfillments.tracking_note/cancelled_reason` + defensivamente em `doctors.notes`, `doctor_payouts.{notes,failed_reason,cancelled_reason}`, `doctor_billing_documents.validation_notes`. **Fecha [9.1] totalmente.**
  - Onda 2F (D-056): **PR-037 (guardrails pra agentes de IA + blindagem `customers.name`)** — primitivas `prompt-envelope.ts` (wrapUserInput com nonce + formatStructuredFields), `prompt-redact.ts` (CPF/CEP/email/phone/UUID/Asaas token/JWT), `customer-display.ts` (displayFirstName/FullName/PlanName/CityState com fallback seguro); `fulfillment-messages.ts` refatorado pra usar os `display*` + helper `safeOpNote`; `/api/checkout` e `/api/agendar/reserve` agora rodam `sanitizeShortText` com pattern `personName`; migration `20260504000000` com backfill idempotente + CHECK em `customers.name` (char_length ≤ 120, POSIX `[[:cntrl:]]`); `AGENTS.md` no root do repo (contrato normativo lido pelos agentes). **76 testes novos. Fecha [9.2] e [9.4].**
  - PR-039 (D-057): **logger canônico estruturado + migração inicial** — `src/lib/logger.ts` zero-deps (JSON em prod, pretty em dev, silencioso em test, `redactForLog` automática via D-056, child loggers com contexto encadeável, sink pluggable). Migração dos caminhos críticos: `cron-runs`, `cron-auth`, `admin-audit-log`, `patient-access-log`, `retention`, `patient-lgpd-requests`, 8 rotas `/api/internal/cron/*`, `/api/asaas/webhook` (29 call-sites). **23 testes novos.** `AGENTS.md` atualizado com o pipeline de logging. **Finding [14.1] rebaixado de 🟠 ALTO pra 🟡 PARCIAL** — infra pronta; plugar drain externo (Axiom/Sentry) aguarda input operacional.
  - PR-042 (D-058): **`fetchWithTimeout` canônico + migração de fetches externos** — `src/lib/fetch-timeout.ts` zero-deps (drop-in replacement do `fetch()` com timeout por `AbortController`, `FetchTimeoutError` classificado, composição com AbortSignal externo, log via logger canônico D-057, `PROVIDER_TIMEOUTS` calibrados). Migrado em `asaas.ts::request` (10s), `whatsapp.ts::postToGraph` (8s), `video.ts::dailyRequest` (8s), `cep.ts::fetchViaCep` (2.5s), `system-health.ts::checkAsaasEnv/checkDailyEnv`. **12 testes novos.** **Finding [13.1] ✅ RESOLVED** — total de ALTOs cai de 6 pra 5.
  - PR-040 (D-059): **dashboard temporal de `cron_runs` em `/admin/crons`** — `src/lib/cron-dashboard.ts` (IO isolado + agregação pura testável + orquestrador) com `CronJobSummary` trazendo `success_rate`, `duration.{avg,p50,p95,max}`, `week_delta.success_rate_delta_pp`, `stuck_count` (running ≥ 2h), `daily[30]` buckets UTC, `recent_runs[20]`. Page `src/app/admin/(shell)/crons/page.tsx` com range 7/30/90d, cards de resumo global, card por job com sparkline (divs zero-deps verde/vermelho), badge de estado, delta semana-vs-semana e `<details>` das últimas 20 execuções. `expectedJobs` injetado mantém crons de cadência baixa visíveis mesmo sem runs recentes. Nav "Crons" adicionado. **20 testes novos.** Sem finding associado (melhoria operacional). Próximo: alertar Slack/WA quando `stuck_count > 0` depende de PR-043.
  - PR-041 (D-060): **bump `next@14.2.18 → 14.2.35` + eslint-config-next matching** — elimina warning `Next.js (14.2.18) is outdated` e, mais importante, **fecha o CVE-2025-29927 (CVSS 9.1 CRÍTICO)**, bypass de autorização em middleware via header `x-middleware-subrequest`. Dado que `src/middleware.ts` é o hard-gate de `/admin/*`, `/medico/*`, `/paciente/*`, havia superfície real (embora mitigada por defense-in-depth `requireAdmin/Doctor/Patient` nos Server Components). Validação: `tsc --noEmit` 0 erros, 936/936 testes, ESLint 0 warnings, smoke HTTP em home/`/admin/login`/`/paciente/login`/`/medico/login`, e **teste empírico do bypass** (`curl -H "x-middleware-subrequest: middleware:middleware:middleware:middleware:middleware" /admin` → 307 redirect, NÃO bypassou). **Finding [11.1] ✅ RESOLVED** — total de ALTOs cai pra 4. Follow-up identificado: `npm audit` mostra 4 `high` residuais na linha 14.x (Image Optimizer DoS, RSC deserialization, rewrite smuggling, next/image cache) cuja fix é só em Next 15.x+ → criado **PR-041-B** (migração Next 14 → 15 com breaking changes de App Router APIs async).
  - PR-055 (D-066): **audit trail de signed URLs de Storage** — fecha finding [17.4 🟠 ALTO]. Tabela imutável `document_access_log` (migration `20260508000000`) com `actor_kind` tri-estado ('admin','doctor','system') + binding constraint, `resource_type` ('payout_proof','billing_document'), `resource_id`=doctor_payouts, `doctor_id` denormalizado, `storage_path`, `signed_url_expires_at`, `action` ('signed_url_issued','external_url_returned'), `ip`/`user_agent`/`route`/`metadata`, 4 índices forenses, RLS deny-all. Lib `src/lib/signed-url-log.ts` com `logSignedUrlIssued` failSoft + `buildSignedUrlContext`. Integrado nos 4 GETs: admin/medico × proof/billing-document. URLs externas legadas também auditadas (action=`external_url_returned`). TTL mantido em 60s (já era). failSoft: INSERT cair não bloqueia o download (privar a médica do próprio RPA é pior que lacuna de log). **14 testes novos; suíte 1031/1031.** Bullets (b) TTL curtíssimo e (c) on-demand do audit já estavam atendidos; bullet (a) proxy stream fica como **PR-055-B** opcional (exige mudar UI de `<a href>` pra fetch+blob). **Finding [17.4] ✅ RESOLVED — ALTOs não-AI zerados.**
  - PR-054 (D-065): **guard de customer takeover em `/api/checkout` e `/api/agendar/reserve`** — fecha findings [3.5 🟠], [3.6 🟠] e [5.8 🟠] (mesma classe de bug em três entradas da auditoria). Lib `src/lib/customer-pii-guard.ts` com `decideCustomerUpsert({ existing, incoming, sessionUserId })` puro: customer com `user_id IS NOT NULL` (paciente já fez magic-link) só permite UPDATE de PII (`name/email/phone/address_*`) se a sessão patient bate com `user_id`; sem sessão ou sessão de outro user → `update_blocked` (PII intocada, cobrança Asaas continua usando dados gravados → atacante não recebe invoice nem WhatsApp). Customer com `user_id IS NULL` (paciente fantasma) segue permitindo UPDATE — risco residual aceito documentado em D-065. Helper `getOptionalPatient()` em `src/lib/auth.ts` (lookup opcional sem redirect, pra rotas POST anônimas). Defesa em profundidade: `createCustomer` Asaas re-busca dados do banco antes de chamar API externa (impede tomber via Asaas mesmo se UPDATE local for bloqueado). Resposta HTTP idêntica em ambos os casos (sem oracle pro atacante). Trilha em `patient_access_log` com novas actions `pii_takeover_blocked` (sempre logada) / `pii_updated_authenticated` / `pii_updated_unauthenticated` (logadas só quando há diff real); `actorKind='system'`; metadata inclui `decision_reason`, `changed_fields[]` ordenado, `route`, `ip`, `user_agent`, `patient_user_id` e (se blocked) `defended_customer_user_id`. **Sem migration** (reusa `customers.user_id` da D-043 + `patient_access_log` da D-051; só estendi tipo TS `PatientAccessAction`). **19 testes novos cobrindo todos os ramos da árvore de decisão + normalização de campos (case email, dígitos phone/zipcode, trim, complement '' vs null, state case) + política de log; suíte 1017/1017.** **Findings [3.5], [3.6], [5.8] ✅ RESOLVED — ALTOs não-AI caem de 1 pra 0 (resta apenas [17.4] signed URL log).** UI deliberadamente não-modificada: avisar usuário "esse CPF tem cadastro fortalecido" vira oracle pro atacante.
  - PR-053 (D-064): **persistência server-authoritative do aceite LGPD em `/api/checkout`** — fecha finding [5.6 🟠 ALTO]. Tabela imutável `checkout_consents` (customer_id + payment_id + text_version + text_snapshot + text_hash SHA-256 canonical + ip + user_agent + payment_method) com trigger `BEFORE UPDATE/DELETE → raise exception` e RLS deny-by-default; lib `checkout-consent-terms.ts` versiona o texto legal (`v1-2026-05`, LGPD art. 11 II "a" + finalidade + farmácia); lib `checkout-consent.ts` com `recordCheckoutConsent()` server-authoritative (cliente envia só versão — server carrega texto canônico, hasheia, grava) + `extractClientIp()` Vercel/CF/XFF; `/api/checkout` rejeita versão desconhecida e ABORTA o checkout se o insert do consent falhar (preferível frustrar cliente legítimo a cobrar sem base legal); `CheckoutForm.tsx` envia `consentTextVersion`; migration `20260507000000_checkout_consents.sql`. **21 testes novos; suíte 998/998.** **Finding [5.6] ✅ RESOLVED — ALTOs não-AI caem de 2 pra 1.**
  - PR-052 (D-063): **retenção + redação PII em `asaas_events`** — fecha finding [5.12 🟠 ALTO]. Política dois-estágios: (1) INSERT-time redact em `src/lib/asaas-event-redact.ts` (allowlist deny-by-default: preserva só id/event/dateCreated + payment.{id,status,billingType,value,netValue,dueDate,paymentDate,externalReference,invoiceNumber,subscription,installment,customer[só id/externalRef],refunds[só metadados],discount/fine/interest[só value/type/dueDateLimitDays],pixTransaction[só qrCode/endToEnd/txid],URLs}; dropa creditCard/creditCardHolderInfo/payer/billing/metadata/customFields/description; PII do customer expandido totalmente removida); (2) Purge pós-180d em `src/lib/asaas-events-retention.ts` + cron weekly `asaas-events-purge` (domingo 05:00 UTC) seta `payload={}::jsonb` + `payload_purged_at=now()` preservando `asaas_event_id/event_type/asaas_payment_id/processed_at` pra rastreio. Migration `20260506000000_asaas_events_retention.sql` adiciona `payload_redacted_at/payload_purged_at` + partial index `asaas_events_purge_candidates_idx`. Webhook `/api/asaas/webhook` chama `redactAsaasPayload()` antes do INSERT; `vercel.json` + dashboard `/admin/crons` atualizados; `CronJob` type expandido. **21 testes novos (12 redact + 9 retention); suíte 977/977.** **Finding [5.12] ✅ RESOLVED — total de ALTOs cai de 3 pra 2.**
  - PR-051 (D-062): **reconciliação pós-clawback no `generateMonthlyPayouts`** — `src/lib/monthly-payouts.ts` ganha loop bounded (max 3 iter) após o link inicial: re-`SELECT` earnings available/unlinked do doctor → `UPDATE` link no payout corrente com guard `status='available' AND payout_id IS NULL` → incorpora sum real. Se extras foram linkados, `UPDATE doctor_payouts SET amount_cents=final, earnings_count=final WHERE status='draft'` + warning `clawback_reconciled`. Se `sum ≤ 0` (clawback dominou), `UPDATE doctor_payouts SET status='cancelled'` + libera earnings (`payout_id=NULL, status='available'`) + warning `clawback_dominant_cancelled` + reverte stats. Se não converge em 3 iter: warning `reconcile_incomplete`; próximo ciclo pega. **3 testes novos em `monthly-payouts.test.ts` (reconciled, dominant cancelled, incomplete); total do arquivo 17/17. Suíte global 956/956.** **Finding [5.5] ✅ RESOLVED.**
  - PR-050 (D-061): **circuit breaker in-memory pra providers externos** — `src/lib/circuit-breaker.ts` zero-deps, 3 estados clássicos (CLOSED/OPEN/HALF_OPEN), rolling window 60s, threshold 50%, minThroughput 5, cooldown 30s. Integrado em 4 providers: `asaas.ts::request`, `whatsapp.ts::postToGraph`, `video.ts::dailyRequest`, `cep.ts::fetchViaCep`. HTTP 5xx contabilizado como falha; 4xx (erro do nosso request) não marca. Cron skip: migration `20260505000000_cron_runs_skipped.sql` adiciona `'skipped'` ao CHECK de `cron_runs.status`; helpers `skipCronRun` + `skipIfCircuitOpen` em `src/lib/cron-guard.ts` integrados em `admin-digest`, `nudge-reconsulta`, `notify-pending-documents` (crons 100% WA-dependentes). Não integrado em `auto-deliver-fulfillments` (trabalho principal é SQL) nem em `wa-reminders` (fail-fast por request já cobre). Observability: `system-health.ts::checkCircuitBreakers` + chip `skipped` no `/admin/crons` + `skipped_count`/bucket `skipped` no `cron-dashboard.ts`. **17 testes unitários (cobrindo estados, transições, rolling window, HALF_OPEN single-probe, registry global); 953/953 total.** **Finding [13.2] ✅ RESOLVED — total de ALTOs cai de 4 pra 3.** Follow-ups: **PR-050-C** (alerta proativo via drain externo — depende de PR-043).
  - PR-039-cont (D-057): **finalização da migração `console.* → logger`** — varredura e migração dos ~150 call-sites remanescentes em libs (`reliability`, `no-show-policy`, `scheduling`, `refunds`, `reconcile`, `reconciliation`, `notifications`, `earnings`, `fulfillment-*`, `patient-update-shipping`, `doctor-finance`, `billing-documents`, `payout-proofs`, `notify-pending-documents`, `video`), rotas API (fulfillment transitions, shipping, confirm-delivery, cancel, billing-document POST/DELETE/validate, payouts proof, medico/profile, doctors, auth, lead, reserve, notifications, appointments join, LGPD anonymize/export/request/reject/fulfill, admin/pacientes search/export), webhooks (`daily`, `wa`, `daily-webhook` legacy) e páginas server component (`/consulta`, `/paciente/oferta`, `/admin/notifications`, `/admin/refunds`, `/admin/payouts`, `/medico/ganhos`, `/admin/fulfillments[/:id]`, `/checkout`, `/agendar`, `/planos`, `/admin/doctors`). **Único `console.*` restante em `src/lib/logger.ts` (implementação interna, esperado).** Testes: 936/936 passando; um `errSpy` em `reconciliation.test.ts` removido pois logger é silencioso em test. **Finding [14.1] continua 🟡 PARCIAL (migração in-tree 100% completa); sai pra ✅ RESOLVED quando PR-043 plugar drain externo.**
- 🔜 Próximos sem input:
  1. **PR-068-B** — auto-block opcional de paciente com 3+ eventos ativos em 90d (`customers.reliability_blocked_at`), bloqueia checkout/agendar. Esperar 3+ meses de dados reais.
  2. **PR-071-B** — remoção definitiva do enum value `pending_payment` de `appointments.status`. Pré-condição: ≥180 dias consecutivos com `LEGACY_PURCHASE_ENABLED=false` sem exceção + contagem zero de linhas históricas ou migração dessas linhas pra `cancelled_by_admin`. Baixa prioridade enquanto o watchdog D-079 estiver silencioso.
  3. **PR-033-Clinical** — retenção pós-20-anos para pacientes com prontuário (só relevante em 2045+).
  4. **PR-041-C** — migração Next 15 → 16 (quando e se houver advisory específico). Delta bem menor que 14→15 (async Request APIs já pagos).
- ⏸️ Bloqueados por input operacional não-crítico:
  - **PR-043** — plugar drain externo no `logger` (`setSink(axiomSink)` + alertas Slack/WA). Precisa: chaves Axiom/Sentry + budget aprovado.
- ⏸️ Bloqueados pelo operador: PR-023 (footer CNPJ/RT — **crítico, bloqueia tráfego pago**), PR-033-B (DPA farmácia), PR-038 (2FA), PR-047 (break-glass)
- ⏸️ Bloqueados por maturação operacional: PR-046 (multi-médica, só quando chegar a 2ª)

**Estado dos CRÍTICOS:** dos 11 CRÍTICOS originais do audit, 10 estão resolvidos (1.1, 2.1/1.3/8.2, 5.1, 5.2, 5.3, 6.1, 10.1, 17.1, 3.x). O único pendente é [7.1] (bloqueado pelo operador — aguardando CNPJ/RT).

**Estado dos ALTOs / MÉDIOs da família 9.x (AI/prompt-injection):** 100% fechados. Findings 9.1 (Ondas 2C+2D+2E), 9.2 (Onda 2F), 9.3 (Onda 2D), 9.4 (Onda 2F). Quando LLM externo for plugado, as primitivas (`prompt-envelope.ts`, `prompt-redact.ts`, `customer-display.ts`) + check-list em `AGENTS.md` são as referências normativas. Tudo sem débito técnico.

**Estado dos demais ALTOs:** fechados também 22.1 (ViaCEP), 22.2 (leads DoS), 13.1 (fetchWithTimeout — PR-042), **11.1 (Next bump + CVE-2025-29927 fix — PR-041)**, **13.2 (circuit breaker — PR-050)**, **5.5 (reconciliação pós-clawback — PR-051)**, **5.12 (asaas_events retention + PII redact — PR-052)**, **5.6 (checkout consent persistido — PR-053)**, **3.5 / 3.6 / 5.8 (customer takeover guard — PR-054)**, **17.4 (signed URL log — PR-055)**, LGPD 6.3/6.4/17.3/Art. 16. 14.1 continua 🟡 PARCIAL (D-057, infra pronta, aguarda drain externo via PR-043). **Zero ALTOs não-AI pendentes.**

Ou seja: **zero CRÍTICOS sem caminho de resolução ativo, zero ALTOs LGPD, zero ALTOs trust-boundary externo, zero ALTOs DoS, zero ALTOs prompt-injection, zero ALTOs/MÉDIOs AI-agent**. Toda família 9.x (AI adversário) fechada pra superfície atual. Quando os inputs do operador chegarem, PR-023 entra **com prioridade máxima** (pré-requisito para qualquer tráfego pago).
